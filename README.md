# RouteMaster-
**RouteMaster** is a web app that calculates the shortest path for warehouse pickers in a grid with obstacles. It uses **BFS/A*** for pathfinding and **TSP optimization** to determine the best order to collect items, visualizing the optimized route on an interactive grid.
